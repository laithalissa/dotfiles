# Load Laithisms
. "$DOTFILES/.aliases"

# Nasty fat hobitsis keeps his secret keys here
. "$DOTFILES/.secrets"

# Colorize maven
. "$SCRIPTS/colourise-mvn"

# Util function
. "$SCRIPTS/functions/source-me"

# Docker wrapped tools
. ~/Code/docker/source-me

# See ~/.zshenv for env var definitions
# Source the gmg_vars script to get service env vars
. ~/Code/fabric/gmg_vars.sh

if [ -f ~/.env_namespace ]; then
  . ~/.env_namespace
fi

if [ $WORK_NAMESPACE ]; then
  . "$DOTFILES/.zshrc_work"
fi
